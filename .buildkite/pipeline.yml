# The connected Buildkite pipeline uses this file as its meta pipeline.
# It builds first, then publishes the artifact only after that build succeeds.

env:
  BUILD_PIPELINE_SLUG: "buildkite-llama-cpp-cuda-build"
  ARTIFACT_PIPELINE_SLUG: "buildkite-llama-cpp-cuda-artifact-upload"

steps:
  - label: ":hammer_and_wrench: Build package pipeline"
    key: "build-package"
    trigger: "${BUILD_PIPELINE_SLUG}"
    async: false
    build:
      message: "${BUILDKITE_MESSAGE}"
      commit: "${BUILDKITE_COMMIT}"
      branch: "${BUILDKITE_BRANCH}"
      env:
        LLAMA_TAG: "${LLAMA_TAG}"
      meta_data:
        triggered_by_pipeline: "${BUILDKITE_PIPELINE_SLUG}"

  - label: ":package: Publish artifact pipeline"
    key: "publish-artifact"
    depends_on: "build-package"
    trigger: "${ARTIFACT_PIPELINE_SLUG}"
    async: false
    build:
      message: "${BUILDKITE_MESSAGE}"
      commit: "${BUILDKITE_COMMIT}"
      branch: "${BUILDKITE_BRANCH}"
      env:
        LLAMA_TAG: "${LLAMA_TAG}"
      meta_data:
        triggered_by_pipeline: "${BUILDKITE_PIPELINE_SLUG}"
